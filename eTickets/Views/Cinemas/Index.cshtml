@model IEnumerable<Cinema>

@{
    ViewData["Title"] = "List of Cinemas";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center rounded-top-4">
            <h3 class="mb-0"><i class="bi bi-camera-reels me-2"></i>@ViewData["Title"]</h3>
            <a asp-action="Create" asp-controller="Cinemas" class="btn btn-light btn-sm fw-bold">
                <i class="bi bi-plus-circle me-1"></i> Add New
            </a>
        </div>
        <div class="card-body">
            <table class="table table-hover align-middle text-center">
                <thead class="table-light">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Logo)</th>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Description)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var cinema in Model)
                    {
                        <tr class="table-row">
                            <td>
                                <img class="rounded-circle shadow-sm" src="@cinema.Logo" alt="@cinema.Name"
                                     style="width:80px; height:80px; object-fit:cover;" />
                            </td>
                            <td>@cinema.Name</td>
                            <td class="text-muted text-truncate" style="max-width: 300px;">@cinema.Description</td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-controller="Cinemas" asp-route-id="@cinema.Id" class="btn btn-outline-primary btn-sm" title="Edit">
                                        <i class="bi bi-pencil-square"></i>
                                    </a>
                                    <a asp-action="Details" asp-controller="Cinemas" asp-route-id="@cinema.Id" class="btn btn-outline-info btn-sm" title="Details">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Cinemas" asp-route-id="@cinema.Id" class="btn btn-outline-danger btn-sm" title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@* <style>
    .table-row:hover {
        background-color: #f9f9f9;
        transform: scale(1.01);
        transition: all 0.2s ease-in-out;
    }

    .card {
        border-radius: 20px;
    }

    .floating-add-btn {
        position: fixed;
        bottom: 30px;
        right: 30px;
        z-index: 999;
    }

        .floating-add-btn .btn {
            border-radius: 50%;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.25);
            transition: all 0.3s;
        }

            .floating-add-btn .btn:hover {
                transform: scale(1.1);
            }
</style> *@

@* Optional floating button version instead of header one *@
@*
<div class="floating-add-btn">
    <a asp-controller="Cinemas" asp-action="Create" class="btn btn-success">
        <i class="bi bi-plus-lg"></i>
    </a>
</div>
*@
